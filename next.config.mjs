/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      unoptimized: true, // Disables image optimization, allowing images from any domain
    },
  };

export default nextConfig;